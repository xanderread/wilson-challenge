import { FileReader, Document } from '@llamaindex/core/schema';

/**
 * Read the text of a PDF
 */
declare class PDFReader extends FileReader<Document> {
    loadDataAsContent(content: Uint8Array): Promise<Document[]>;
}

export { PDFReader };
