/**
 * The underlying structure of each index.
 */
export declare abstract class IndexStruct {
    indexId: string;
    summary?: string | undefined;
    constructor(indexId?: `${string}-${string}-${string}-${string}-${string}`, summary?: string | undefined);
    toJson(): Record<string, unknown>;
    getSummary(): string;
}
